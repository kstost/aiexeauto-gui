{
    "name": "run_command",
    "description": "Run a shell command.",
    "input_schema": [
        {
            "command": ""
        },
        {
            "command": "shell command to run, e.g, ls -al"
        }
    ],
    "available_for": [
        "claude",
        "openai",
        "ollama"
    ]
}